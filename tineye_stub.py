def reverse_search_hint():
    return (
        "Use Google Images (Images -> camera icon) or TinEye to reverse-search the profile picture. "
        "This tool cannot perform the search automatically; upload the saved profile image from the downloads folder."
    )
